# fly.toml app configuration file generated for woodpecker-relay on 2026-07-08T14:12:42+08:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'woodpecker-relay'
primary_region = 'syd'

# IMPORTANT: this app must run on EXACTLY ONE machine. Channels live in
# process memory, so with 2+ machines Fly load-balances the connector and
# the canvas onto different instances and they never see each other.
# (`fly deploy --ha=false`; check with `fly scale show`.) If HA is ever
# needed, channel state must move to shared storage first.

[build]
[http_service]
  internal_port = 9000
  force_https = true
  auto_stop_machines = 'off'
  auto_start_machines = true
  min_machines_running = 1

[checks]
  [checks.health]
    port = 9000
    type = 'http'
    interval = '30s'
    timeout = '5s'
    path = '/healthz'

[[vm]]
  size = 'shared-cpu-1x'
  memory = '256mb'
  cpus = 1
  memory_mb = 256
