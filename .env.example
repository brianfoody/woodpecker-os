# WebSocket relay the canvas uses to reach paired connectors.
# Leave the default for production; point at ws://localhost:9000 when
# running `node relay/src/server.js` locally.
NEXT_PUBLIC_RELAY_URL=wss://relay.woodpeckeros.com
